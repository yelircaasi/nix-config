local legendary = require("legendary")
local which_key = require("which-key")
local hydra = require("hydra")
local keymap_amend = require("keymap-amend")

return legendary, which_key, hydra, keymap_amend

